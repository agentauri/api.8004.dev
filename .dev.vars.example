# Local Development Variables for Cloudflare Workers
# Copy this file to .dev.vars for local development with wrangler dev
# DO NOT commit .dev.vars to version control

# =============================================================================
# REQUIRED SECRETS
# =============================================================================

# Google AI API key for OASF classification (primary)
GOOGLE_AI_API_KEY=AIza...

# Anthropic API key for OASF classification (fallback)
ANTHROPIC_API_KEY=sk-ant-api03-xxxxx

# Semantic search service URL (legacy, still used for fallback)
SEARCH_SERVICE_URL=https://agent0-semantic-search.example.workers.dev

# RPC URLs for blockchain access
SEPOLIA_RPC_URL=https://eth-sepolia.g.alchemy.com/v2/your-api-key
BASE_SEPOLIA_RPC_URL=https://base-sepolia.g.alchemy.com/v2/your-api-key
POLYGON_AMOY_RPC_URL=https://polygon-amoy.g.alchemy.com/v2/your-api-key

# Qdrant Cloud for vector search
QDRANT_URL=https://your-cluster.region.gcp.cloud.qdrant.io
QDRANT_API_KEY=your-qdrant-api-key

# Venice AI for embeddings
VENICE_API_KEY=your-venice-api-key

# The Graph API key for subgraph queries
GRAPH_API_KEY=your-graph-api-key

# =============================================================================
# OPTIONAL CONFIGURATION
# These have defaults but can be overridden
# =============================================================================

# Environment name (development, staging, production)
# ENVIRONMENT=development

# Cache TTL in seconds (default: 300)
# CACHE_TTL=300

# Rate limit requests per minute (default: 100)
# RATE_LIMIT_RPM=100

# Primary model for classification (default: gemini-2.0-flash)
# CLASSIFICATION_MODEL=gemini-2.0-flash

# Fallback model for classification (default: claude-3-haiku-20240307)
# FALLBACK_MODEL=claude-3-haiku-20240307

# Embedding model for vector search (default: text-embedding-bge-m3 for Venice)
# EMBEDDING_MODEL=text-embedding-bge-m3

# Qdrant collection name (default: agents)
# QDRANT_COLLECTION=agents
